{
    "name": "@bible-web-framework/backend",
    "scripts": {
        "build": "cargo build --release",
        "lint": "cargo clippy --all-targets && cargo fmt --check",
        "lint:fix": "cargo clippy --all-targets --fix --allow-dirty && cargo fmt",
        "dev": "cargo run"
    }
}